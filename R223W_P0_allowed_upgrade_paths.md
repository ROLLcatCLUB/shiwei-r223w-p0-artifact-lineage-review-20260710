# R223W-P0 allowed upgrade paths

## 1. Teacher manuscript quality issue

Examples:

- teacher draft reads awkwardly
- classroom process is shallow
- design intent is weak
- assessment language needs polish
- lesson structure is unclear

Allowed route:

```text
Return to R223M / R223N / R223O teacher manuscript line.
```

Not allowed:

```text
Do not fix teacher body inside R223T/W/X sandbox shell.
```

## 2. v0.2 schema or field issue

Examples:

- practice_pattern_type is insufficient
- unit intensity router lacks a value
- review ledger loses component status
- evidence mapping needs schema change

Allowed route:

```text
Return to R223P/Q schema and generation regression line.
```

Not allowed:

```text
Do not patch schema semantics inside a preview page.
```

## 3. Sandbox preview readability issue

Examples:

- ledger too heavy
- comparison summary too long
- safety banner unclear
- component metadata looks executable

Allowed route:

```text
Modify R223T/W sandbox or review shell only.
```

Not allowed:

```text
Do not create a new teacher draft baseline.
```

## 4. Formal UI issue

Examples:

- R97B carrier layout
- formal route behavior
- official teacher-facing navigation
- product integration

Allowed route:

```text
Future R97B / formal UI line.
```

Current status:

```text
FORMAL_UI = BLOCKED
R97B_ROUTE = BLOCKED
runtime / model / prompt / db = BLOCKED
```

